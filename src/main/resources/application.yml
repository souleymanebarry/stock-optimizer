spring:
  application:
    name: stock-optimizer
  h2:
    console:
      enabled: true
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:stock-optimizer-db
    driverClassName: org.h2.Driver
  jpa:
    show-sql: true
